import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        backgroundColor: '#000',
        padding: 20,
        paddingStart: 40,
        textAlign: 'center'
    },
    texto:{
        fontSize: 36,
        fontStyle: 'italic',
        fontWeight: 'bold',
        color: '#fff'
    }
});

export default styles;